{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "React: Inbox",
      "type": "node-terminal",
      "request": "launch",
      "command": "cd ${workspaceFolder}/web/react/inbox && npm run dev -- --open"
    },
    {
      "name": "Curl: Send Template to Email",
      "type": "node-terminal",
      "request": "launch",
      "command": "cd ${workspaceFolder}/server-side/curl && bash send-template-to-email.sh"
    }
  ]
}

